import { StyleSheet, Platform } from "react-native";

const styles = StyleSheet.create({
  safeAreaContainer: {
    flex: 1
  },
  container: {
    flex: 1
  },
  flatList: {
    flex: 1
  }
});

export default styles;
