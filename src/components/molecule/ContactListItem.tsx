import React from "react";
import {
    Text,
    View
} from "react-native";

export const CustomButton = ({}) => {
    return (
        <View style={{ flex: 1 }}>
            <Text style={{ color: "#FFF", textAlign: "center" }}>Temp</Text>
        </View>
    );
}